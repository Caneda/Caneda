PROJECT( Qucs )

SET(BITMAPDIR 1)
SET(BINARYDIR 1)
SET(DOCDIR 1)
SET(LANGUAGEDIR 1)
SET(LIBRARYDIR 1)

ADD_DEFINITIONS( -DHAVE_CONFIG_H )

CONFIGURE_FILE(${Qucs_SOURCE_DIR}/config.h.cmake ${Qucs_SOURCE_DIR}/config.h)

SET( QT_USE_QTXML TRUE )
SET( QT_USE_QTSVG TRUE )
SET( QT_USE_QTCORE TRUE )
SET( QT_USE_QTGUI TRUE )

FIND_PACKAGE( Qt4 REQUIRED )
INCLUDE( ${QT_USE_FILE} )

INCLUDE_DIRECTORIES( ${CMAKE_SOURCE_DIR} ${CMAKE_SOURCE_DIR}/qucs )

ADD_SUBDIRECTORY( qucs-tools )
ADD_SUBDIRECTORY( qucs )
